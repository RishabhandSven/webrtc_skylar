{
  "name": "webrtc-signaling-server",
  "version": "1.0.0",
  "description": "Minimal WebSocket signaling server for WebRTC P2P stream sharing",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
